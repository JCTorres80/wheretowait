<%= form_for(@flagged_word, :html => {:role => 'form'}) do |f| %>
    <% if @flagged_word.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@flagged_word.errors.count, "error") %> prohibited this flagged_word from being saved:</h2>

          <ul>
            <% @flagged_word.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="form-group">
      <%= f.label :word %>
      <br/>
      <%= f.text_field :word,
                       :placeholder => :word,
                       :class => 'form-control' %>
    </div>

    <div class="actions">
      <%= f.submit :class => 'btn btn-primary' %>
    </div>
<% end %>
